# AI Radio — Echo
# Python dependencies

# Core runtime
python-dotenv>=1.0.0

# News fetching
feedparser>=6.0.0
requests>=2.31.0

# AI clients
google-generativeai>=0.7.0
groq>=0.9.0

# TTS
edge-tts>=6.1.9

# Image / video
Pillow>=10.0.0
# FFmpeg is a system binary — install separately (not via pip)

# YouTube upload
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
google-auth-httplib2>=0.1.1

# HTML parsing (stdlib HTMLParser used; no extra dep)
