# AI Radio — Echo — Python dependencies
# Install: pip install -r requirements.txt

# AI providers
google-genai>=0.8.0
groq>=0.9.0

# TTS
edge-tts>=6.1.9

# News fetching
feedparser>=6.0.11
requests>=2.31.0

# Environment
python-dotenv>=1.0.0

# Image generation (optional but recommended — fallback to stdlib PNG if absent)
Pillow>=10.3.0

# YouTube upload
google-api-python-client>=2.134.0
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
